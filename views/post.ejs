<%- include('partials/header', {title: 'Post'}); %>

<body class="text-center">
    <div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column">

        <%- include('partials/navbar_loggedin') -%>

        <main role="main" class="feed">
            <h1 class="cover-heading feed-title"><%= post.title %></h1>
            <p class="lead feed-body"><%= post.body %></p>
            <% if (post.imageSrc) { %>
                <img class="feed-image" src="<%=post.imageSrc %>" alt="Post image">
            <% } %>
            <p class="feed-author"><%= post.author %></p>
        </main>

        <%- include('partials/footer') -%> 
        
    </div>
</body>

</html>